<UserControl x:Class="KryptoSmenarna.Wpf.UserSubWindows.WalletOperationsSubWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <UserControl.Resources>
        <Style x:Key="ModernComboBox" TargetType="ComboBox">
            <Setter Property="Height" Value="40"/>
            <Setter Property="Foreground" Value="#374151"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#D1D5DB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,0,0,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Border x:Name="MainBorder"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="6"/>

                            <Grid Margin="{TemplateBinding Padding}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="32"/>
                                </Grid.ColumnDefinitions>

                                <ContentPresenter Grid.Column="0"
                                                  IsHitTestVisible="False"
                                                  Content="{TemplateBinding SelectionBoxItem}"
                                                  ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                  ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                                  VerticalAlignment="Center"/>

                                <Path Grid.Column="1"
                                      Data="M 0 0 L 4 4 L 8 0"
                                      Stroke="#9CA3AF"
                                      StrokeThickness="2"
                                      VerticalAlignment="Center"
                                      HorizontalAlignment="Center"/>
                            </Grid>

                            <ToggleButton x:Name="ToggleButton"
                                          Background="Transparent"
                                          BorderThickness="0"
                                          IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"
                                          Focusable="False">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border Background="Transparent"/>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>

                            <Popup x:Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}" AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                                <Grid MaxHeight="{TemplateBinding MaxDropDownHeight}" MinWidth="{TemplateBinding ActualWidth}">
                                    <Border x:Name="DropDownBorder" Background="White" BorderBrush="#D1D5DB" BorderThickness="1" CornerRadius="6" Margin="0,4,0,10">
                                        <Border.Effect>
                                            <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.1"/>
                                        </Border.Effect>
                                        <ScrollViewer SnapsToDevicePixels="True" Margin="2">
                                            <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained"/>
                                        </ScrollViewer>
                                    </Border>
                                </Grid>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="MainBorder" Property="BorderBrush" Value="#9CA3AF"/>
                            </Trigger>
                            <Trigger Property="IsDropDownOpen" Value="True">
                                <Setter TargetName="MainBorder" Property="BorderBrush" Value="#4F46E5"/>
                                <Setter TargetName="MainBorder" Property="BorderThickness" Value="2"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderBrush" Value="#D1D5DB"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1" Background="White" CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="false"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#4F46E5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.9"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Border Background="White" CornerRadius="12" Padding="24" Width="350" Margin="10">
        <Border.Effect>
            <DropShadowEffect BlurRadius="25" ShadowDepth="2" Opacity="0.06"/>
        </Border.Effect>

        <StackPanel>
            <TextBlock Text="Moje peněženky" FontSize="20" FontWeight="Bold" Foreground="#111827" Margin="0,0,0,24"/>

            <StackPanel Margin="0,0,0,24">
                <TextBlock Text="FIAT ÚČET" FontSize="11" FontWeight="Bold" Foreground="#6B7280" Margin="0,0,0,8"/>
                <ComboBox x:Name="ComboboxFiat" Style="{StaticResource ModernComboBox}" DisplayMemberPath="currencyCode" SelectionChanged="ComboboxFiat_SelectionChanged" Margin="0,0,0,12"/>
                <TextBlock x:Name="TextBlockFiatBalance" Text="125 400.00 CZK" FontSize="24" FontWeight="Bold" Foreground="#111827" Margin="0,0,0,16"/>

                <TextBlock Text="ČÁSTKA" FontSize="11" FontWeight="Bold" Foreground="#6B7280" Margin="0,0,0,4"/>
                <TextBox x:Name="TextBoxFiatOperation" Style="{StaticResource ModernTextBox}" Text="0" Margin="0,0,0,16"/>

                <UniformGrid Columns="2">
                    <Button Content="Vklad" Height="40" Style="{StaticResource ModernButton}" Background="#4F46E5" Foreground="White" FontWeight="SemiBold" Click="ButtonFiatDeposit_Click"/>
                    <Button Content="Výběr" Height="40" Style="{StaticResource ModernButton}" Background="#F9FAFB" Foreground="#374151" BorderBrush="#D1D5DB" BorderThickness="1" Margin="12,0,0,0" Click="ButtonFiatWithdraw_Click"/>
                </UniformGrid>
            </StackPanel>

            <Separator Background="#F3F4F6" Margin="0,0,0,24"/>

            <StackPanel>
                <TextBlock Text="KRYPTO AKTIVUM" FontSize="11" FontWeight="Bold" Foreground="#6B7280" Margin="0,0,0,8"/>
                <ComboBox x:Name="ComboBoxCrypto" Style="{StaticResource ModernComboBox}" DisplayMemberPath="currencyCode" SelectionChanged="ComboBoxCrypto_SelectionChanged" Margin="0,0,0,12"/>

                <TextBlock x:Name="TextBlockCryptoBalance" Text="0.4528 BTC" FontSize="24" FontWeight="Bold" Foreground="#111827"/>
                <TextBlock x:Name="TextBlockCryptoInFiat" Text="≈ 640 000 CZK" FontSize="14" Foreground="#6B7280" Margin="0,2,0,16"/>

                <TextBlock Text="ČÁSTKA" FontSize="11" FontWeight="Bold" Foreground="#6B7280" Margin="0,0,0,4"/>
                <TextBox x:Name="TextBoxCryptoOperation" Style="{StaticResource ModernTextBox}" Text="0.00" Margin="0,0,0,16"/>

                <UniformGrid Columns="2">
                    <Button Content="Vklad" Height="40" Style="{StaticResource ModernButton}" Background="#F59E0B" Foreground="White" FontWeight="SemiBold" Click="ButtonCryptoDeposit_Click"/>
                    <Button Content="Výběr" Height="40" Style="{StaticResource ModernButton}" Background="#F9FAFB" Foreground="#374151" BorderBrush="#D1D5DB" BorderThickness="1" Margin="12,0,0,0" Click="ButtonCryptoWithdraw_Click"/>
                </UniformGrid>
            </StackPanel>
        </StackPanel>
    </Border>
</UserControl>
